version: 2

sources:

  - name: gcp_billing
    description: "Data source containing billing information extracted from Google BigQuery."
    schema: sandbox_varejo_billing
    tables:
      - name: gcp_billing_export_resource_v1_017006_67EE6C_8DAC3D
        description: '{{ doc("gcp_billing_export_resource_v1_<BILLING_ACCOUNT_ID>") }}'
        columns:
          - name: billing_account_id
            description: '{{ doc("billing_account_id") }}'

          - name: service.id
            description: '{{ doc("service.id") }}'

          - name: service.description
            description: '{{ doc("service.description") }}'

          - name: sku.id
            description: '{{ doc("sku.id") }}'

          - name: sku.description
            description: '{{ doc("sku.description") }}'

          - name: usage_start_time
            description: '{{ doc("usage_start_time") }}'

          - name: usage_end_time
            description: '{{ doc("usage_end_time") }}'

          - name: billing_date
            description: '{{ doc("billing_date") }}'

          - name: project.id
            description: '{{ doc("project.id") }}'

          - name: project.number
            description: '{{ doc("project.number") }}'

          - name: project.name
            description: '{{ doc("project.name") }}'

          - name: project.labels.key
            description: '{{ doc("project.labels.key") }}'

          - name: project.labels.value
            description: '{{ doc("project.labels.value") }}'

          - name: project.ancestry_numbers
            description: '{{ doc("project.ancestry_numbers") }}'

          - name: project.ancestors.resource_name
            description: '{{ doc("project.ancestors.resource_name") }}'

          - name: project.ancestors.display_name
            description: '{{ doc("project.ancestors.display_name") }}'

          - name: labels.key
            description: '{{ doc("labels.key") }}'

          - name: labels.value
            description: '{{ doc("labels.value") }}'

          - name: system_labels.key
            description: '{{ doc("system_labels.key") }}'

          - name: system_labels.value
            description: '{{ doc("system_labels.value") }}'

          - name: location.location
            description: '{{ doc("location.location") }}'

          - name: location.country
            description: '{{ doc("location.country") }}'

          - name: location.region
            description: '{{ doc("location.region") }}'

          - name: location.zone
            description: '{{ doc("location.zone") }}'

          - name: resource.name
            description: '{{ doc("resource.name") }}'

          - name: resource.global_name
            description: '{{ doc("resource.global_name") }}'

          - name: tags.key
            description: '{{ doc("tags.key") }}'

          - name: tags.value
            description: '{{ doc("tags.value") }}'

          - name: tags.inherited
            description: '{{ doc("tags.inherited") }}'

          - name: tags.namespace
            description: '{{ doc("tags.namespace") }}'

          - name: export_time
            description: '{{ doc("export_time") }}'

          - name: cost
            description: '{{ doc("cost") }}'

          - name: currency
            description: '{{ doc("currency") }}'

          - name: currency_conversion_rate
            description: '{{ doc("currency_conversion_rate") }}'

          - name: usage.amount
            description: '{{ doc("usage.amount") }}'

          - name: usage.unit
            description: '{{ doc("usage.unit") }}'

          - name: usage.amount_in_pricing_units
            description: '{{ doc("usage.amount_in_pricing_units") }}'

          - name: usage.pricing_unit
            description: '{{ doc("usage.pricing_unit") }}'

          - name: credits.name
            description: '{{ doc("credits.name") }}'

          - name: credits.amount
            description: '{{ doc("credits.amount") }}'

          - name: credits.full_name
            description: '{{ doc("credits.full_name") }}'

          - name: credits.id
            description: '{{ doc("credits.id") }}'

          - name: credits.type
            description: '{{ doc("credits.type") }}'

          - name: invoice.month
            description: '{{ doc("invoice.month") }}'

          - name: cost_type
            description: '{{ doc("cost_type") }}'

          - name: adjustment_info.id
            description: '{{ doc("adjustment_info.id") }}'

          - name: adjustment_info.description
            description: '{{ doc("adjustment_info.description") }}'

          - name: adjustment_info.mode
            description: '{{ doc("adjustment_info.mode") }}'

          - name: adjustment_info.type
            description: '{{ doc("adjustment_info.type") }}'

      - name: dbt_utils_days
        description: '{{ doc("dbt_utils_days") }}'